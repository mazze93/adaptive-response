packages:
  - 'packages/*'
  - 'apps/*'

allowBuilds:
  esbuild: true
  sharp: true
  workerd: true

overrides:
  esbuild: ">=0.28.1"
  undici: ">=7.28.0"
  ws: ">=8.21.0"
  postcss: ">=8.5.18"
  "@babel/core": ">=7.29.1"
  vite: ">=6.4.3"
  # sharp reaches the tree only via apps/api > wrangler > miniflare, which has
  # no bump carrying the patched libvips (GHSA-f88m-g3jw-g9cj).
  sharp: ">=0.35.0"
